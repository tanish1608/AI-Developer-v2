import React from "react";

function Layout({ children }) {
const containerStyle = {
padding: "20px",
maxWidth: "1200px",
margin: "0 auto",
fontFamily: "Arial, sans-serif",
};

const headerStyle = {
backgroundColor: "#007bff",
color: "#fff",
padding: "10px",
textAlign: "center",
fontSize: "24px",
fontWeight: "bold",
};

const footerStyle = {
backgroundColor: "#007bff",
color: "#fff",
padding: "10px",
textAlign: "center",
fontSize: "18px",
};

return (
<div style={containerStyle}>
<header style={headerStyle}>Todo App</header>
<main>{children}</main>
<footer style={footerStyle}>Copyright 2023 Todo App</footer>
</div>
);
}

export default Layout;
import React from "react";
import Layout from "./Layout";
import AppWrapper from "./AppWrapper";

function App() {
return (
<Layout>
<AppWrapper />
</Layout>
);
}

export default App;